// Create an object representing a person with properties like name, age, and occupation. Print each property.

const person = {
  name: 'Olivia',
  age: '27',
  occupation: 'Dentist'
};

console.log(person.name, person.age, person.occupation);
