// Create variables to store a person's name, age, and favorite color. Print them to the console.

const person = {
  name: 'Ana',
  age: '35',
  color: 'blue'
};
console.log(person.name, person.age, person.color);
